[project]
name = "bg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "pydantic>=2.11.5",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pandas>=2.3.0",
    "pytest>=8.4.0",
]
